export interface UserModel {
  id: number;
  name: string;
  pictureUrl: string;
  currentDateTime: string;
}

export interface BattlenetUserModel {
  sub: string;
  id: number;
  battletag: string;
}

